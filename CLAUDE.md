# InstructionsCrafter — Project Instructions

## What this project is

A tool that turns a screen recording with voice narration into a polished Microsoft Word step-by-step instruction document. A Chrome extension (forked from Screenity) records screen + audio and uploads to a FastAPI backend, which transcribes the audio with OpenAI Whisper, segments it into logical steps, extracts one screenshot per step, polishes the narration with GPT-4o mini, and returns a `.docx` file. No long-term storage.

## Source of truth

**Full specification: `ARCHITECTURE.md`** in the project root.
Read it before making design decisions. Do not rewrite or duplicate the plan it contains.

## Critical principles (from ARCHITECTURE.md — must follow)

1. **Reuse over rewrite.** Assemble existing libraries with glue code. Do not reimplement what reputable libraries already do.
2. **Prefer boring technology.** FastAPI, FFmpeg, python-docx-template, OpenAI APIs. No exotic frameworks.
3. **Target 1,000–1,500 lines of custom code total.** Code growing far beyond this signals over-engineering.
4. **Internal tool for ≤5 users.** No multi-tenancy, RBAC, microservices, or enterprise observability.
5. **No long-term storage.** Videos and generated documents are deleted after each request. No database for user content.
6. **Ask before inventing.** If a requirement is ambiguous, state the interpretation explicitly before implementing.

## Development workflow

Work through the 9 phases in `ARCHITECTURE.md` **sequentially**. Do not start phase N+1 until phase N is verified working end-to-end per its listed verification step.

**Current phase: Phase 8 in progress — Stages A, B, C, D, and E complete. A: Screenity fork imported into `extension/`. B: options page, post-recording Generate page, recording-blob loading from OPFS (materialized into memory at load time) / IndexedDB, multipart upload to `/api/generate`, .docx download. C: welcome page replacing Screenity's `setup.html`, permanent toolbar tooltip stating the narration requirement, audio-silence detection both client-side (hard-blocks the Generate button) and backend-side (ffmpeg `volumedetect` gate at -50 dB rejects silent recordings before Whisper hallucinates filler from them), Download recording button, backend CORS middleware. D (6 sub-commits, ARCHITECTURE.md lines 86–98): D1 deleted `Editor/`, `EditorWebCodecs/`, `EditorViewer/`, `Sandbox/` source dirs + GIF export + crash-recovery (~13k lines); D2 deleted `Background/drive/` + `Background/modules/signIn.js` + `oauth2`/`identity` from manifest; D3 removed the countdown on/off toggle (3-second duration was already hardcoded); D4 removed push-to-talk Switch + Alt+Shift+U listeners + state plumbing; D5 deleted `Camera/`, `Content/camera/`, `Content/camera-only/`, MediaPipe `vision_wasm*` assets (~22 MB), camera tab in popup, camera toggle button in toolbar; D6 moved shared OPFS chunk-store from `CloudRecorder/recorderStorage/` to `Background/chunkStorage/` then deleted `CloudRecorder/` in full (~9k lines) + `OffscreenRecorder/resumeJournal.js` + OffscreenRecorder cloud-branch. E (5 sub-commits, "surface cleanup"): E1 dropped `externally_connectable` from manifest; E2 flattened dead `recordingType === "camera"` branches in `Recorder.jsx`, dead `editorUrl === "editorviewer.html"` branch + duplicate-string ternary in `stopRecording.js`, and three `disabled={...camera}` props on toolbar `ToolTrigger`s; E3 deleted orphan `Setup/` (Stage C replaced its trigger with `Welcome/`) and the now-dead setup.html filter checks across `openRecorderTab.js`, `onActionButtonClickedListener.js`, `onCommandListener.js`, `Wrapper.jsx`, manifest, webpack — kept `Playground/` and `Backup/` after audit (both still actively opened); E4 stripped 173 unused i18n keys × 18 locales (~12k lines deleted; detected via `getMessage("KEY")` cross-reference); E5 npm-uninstalled 8 unused deps (`@mediapipe/tasks-vision`, `gif.js`, `plyr`, `plyr-react`, `react-advanced-cropper`, `fix-webm-duration`, `webm-duration-fix`, `@sentry/browser`) — kept `fabric` (still used by `Content/canvas/` for in-recording annotation overlay). End-to-end verified against local backend. **Deferred to Phase 9 polish — see the "Deferred cleanup" section below.** Next: Phase 7 (deploy backend); the extension's stored `BACKEND_URL` flips from `http://127.0.0.1:8000` to the deployed domain at that point.**
Update this line whenever the current phase changes.

## Deferred cleanup (Phase 9 polish backlog)

After Stage E, ~600–800 lines of inert Screenity cloud-account code remain in the tree. All gated at runtime by `CLOUD_FEATURES_ENABLED=false` (webpack DefinePlugin) or by no-data conditions, so none of it ships reachable behavior. Listed so a future maintainer doesn't re-discover them from scratch — each item names a non-obvious reason it wasn't deleted in Stages D/E.

- **`Background/auth/loginWithWebsite.js`** + callers in `setSurface.js`, `openRecorderTab.js`, `onMessageExternalListener.js` — Screenity web-app login. Returns `{ authenticated: false }` immediately when flag is off. Callers need their `loginWithWebsite()` lines flattened to a literal `false`.
- **`Background/listeners/onMessageExternalListener.js`** — whole listener early-returns on `!CLOUD_FEATURES_ENABLED`. File + its line in `listeners/index.js` are deletable.
- **`Background/recording/restoreCloudRecording.js`** + `restore-cloud-recording` / `check-cloud-restore` handlers in `Background/messaging/handlers.js` + `check-cloud-restore` sender in `Content/popup/layout/SettingsMenu.jsx:270` — cloud session restore; finds no sessions to restore.
- **`Background/recording/resumePendingUploads.js`** + 3 callers in startup / install / action listeners — BunnyTus upload journal resume; finds no journals (its target `OffscreenRecorder/resumeJournal.js` was deleted in D6, so it sends `resume-pending-uploads` to a non-existent listener).
- **`Background/index.js` `cleanupOrphanOpfsSessions`** + its call site — reaps `cloud-chunks/` OPFS directories that no longer exist.
- **Local-playback-offer machinery in `Background/messaging/handlers.js`** (~200 lines: `CLOUD_LOCAL_PLAYBACK_*` constants, `offerScreenStore`, `clearStoredLocalPlaybackOffer`, `scheduleLocalPlaybackAlarm`, message handlers for `cloud-local-playback-*`) + 4–5 senders in `Content/context/messaging/handlers.js`. Largest single deferred chunk.
- **`isLoggedIn` / `isSubscribed` / `screenityToken` / `wasLoggedIn` / `proSubscription` / `hasSubscribedBefore` state plumbing in `Content/*`** — gates ~50+ UI conditional renders in popup, toolbar, settings. Flag-derived state is always `false` in our build. Deferred because flattening every `if (isSubscribed)` cleanly without breaking the popup layout is many-file work for zero behavioral gain.
- **`cameraActive` / `cameraPermission` / `defaultVideoInput` / `backgroundEffects*` state plumbing** — written to storage, read in many places, but no live consumer after D5 deleted the camera UI.
- **Broken `npm run build:content-css` script** — Stage D5 deleted `Content/camera/` and `Content/camera-only/`, but `src/pages/Content/styles/app.scss` still has `@use "../camera/styles/_Camera.scss";` and `@use "../camera-only/styles/CameraOnly.scss";`. The script fails on these missing imports AND **destructively overwrites `app.css` with the sass error message** when it runs. Until fixed, edit `app.css` by hand when removing UI features (or `git restore` it if accidentally clobbered). Fix: delete those two `@use` lines from `app.scss`, then `build:content-css` becomes the canonical regenerator again.
- **Editorial Manual fonts loaded via Google Fonts CDN inside the content-script shadow DOM** (introduced in F38, countdown redesign) — `src/pages/Content/styles/app.css` starts with `@import url("https://fonts.googleapis.com/css2?family=Geist+Mono:wght@400;500&family=Instrument+Serif:ital@0;1&display=swap")`. On host pages with strict CSP that block external font fetches (some banking sites, locked-down Salesforce orgs, etc.), the fetch is blocked and the countdown falls back to `Times New Roman` italic for the numeral and system monospace for the cancel hint. Both fallbacks render acceptably at the sizes we're using, but the numeral isn't visually pixel-identical to the Welcome/Generate pages. Fix: bundle the `.woff2` files locally under `extension/src/assets/fonts/` (mirror the Satoshi pattern already there for Screenity's bundled fonts), declare `@font-face` blocks pointing to `chrome-extension://__MSG_@@extension_id__/assets/fonts/...` URLs at the top of `app.css`, and drop the Google Fonts `@import`. Same fix would benefit any future in-recording UI redesign (toolbar, modal, etc.) that wants the Editorial Manual fonts.
- **`.ToastAction` orphan class in Recorder warning toast** — discovered during F53 (Recorder redesign) audit. `Recorder/warning/Warning.jsx` template-string `<style>` (~10 lines, lines 191–199) defines `.ToastAction` for an action button on the bottom warning toast (`background-color: #51515f`, `Satoshi-Medium`), but no `<ToastEl.Action>` is rendered anywhere in `Warning.jsx` — the toast only has a close button. F53 deliberately left this block in place per the "don't touch dead code" rule; will be deleted in the broader Phase 9 cleanup pass alongside the popup-internal orphans above. (F53 incidentally dropped a peer orphan `.button-stop` block in `Recorder/RecorderUI.jsx` because that file was rewritten in full to add the mono-caps eyebrow + Geist swap; the `.ToastAction` block survives because Warning.jsx was edited surgically with `Edit` rather than rewritten.)
- **Popup-internal Screenity surfaces left after F48–F51** — discovered during the popup-internals audit. All inert in our build because their JSX consumers are cloud-gated, alarm-feature-removed (F31), region-mode-removed (F45 revert), or were orphaned by earlier F-cleanups (F4 tabs, F6 Screen/Tab tabs, F14 Settings collapsible, F31 cog menu). Listed grouped by file:
  - **`extension/src/pages/Content/popup/styles/components/_Tooltip.scss`** (99 lines) + its compiled block in `app.css:3279` — the popup-internal Radix Tooltip. Its only JSX consumer (`popup/components/TooltipWrap.jsx`, used at `popup/layout/RecordingType.jsx:203`) is gated by `CLOUD_FEATURES_ENABLED && contentState.isLoggedIn`. The class name `.TooltipContent` clashes with the F40-redesigned in-recording toolbar Tooltip; F51 harmonized the runtime override in `Content.jsx` so the toolbar tooltip finally renders correctly, but this dead popup .scss block remains in the cascade and would re-shadow F40 if `Content.jsx`'s override is ever removed. Fix: delete the file + the `@use` line in `popup/styles/_Popup.scss` + the `popup/components/TooltipWrap.jsx` file + the `TooltipWrap` import line in `popup/layout/RecordingType.jsx`.
  - **`popup/styles/components/_Switch.scss .ExperimentalLabel`** (~10 lines, lines 78–86) — Beta-feature pill. The `experimental` prop on `<Switch>` is referenced in `popup/components/Switch.jsx:32` but no live caller ever passes `experimental={true}`. Used to flag experimental popup toggles in Screenity (e.g., fast-recorder beta); we removed all such toggles. Fix: delete the `<span className="ExperimentalLabel">` block in `Switch.jsx` and the corresponding .scss block.
  - **`popup/styles/components/_MainButton.scss .alarm-time-button`** (~20 lines, lines 56–74) — Timed-recording countdown chip overlay on the Start button. Gated by `contentState.alarm && contentState.alarmTime > 0` in `popup/layout/RecordingType.jsx:273`. Storage default is `alarm: false` and no live setter flips it true (F31 deleted the cog menu that exposed the alarm setter). Fix: delete the JSX block + the .scss class + the `alarm`/`alarmTime` state plumbing in `ContentState.jsx` + `updateFromStorage.js`.
  - **`popup/styles/components/_MainButton.scss .dashboard-button`** (~10 lines, lines 47–54) — Screenity "Go to dashboard" button styling. Only consumed by `popup/layout/Welcome.jsx:117,149` and `popup/layout/WelcomeAlternate.jsx:80`, both of which are gated by `isCloudBuild`. Fix: delete with the rest of the dead popup Welcome surfaces.
  - **`popup/styles/layout/_PopupContainer.scss .HelpSection`** (~25 lines, lines 198–224, Screenity-era 30px pill in `#edeef2`) — no live JSX consumer at all. Pure orphan, probably from a long-deleted Screenity help-text bar.
  - **`popup/styles/layout/_PopupContainer.scss .popup-warning-right`** + the second `.popup-warning` JSX block in `popup/layout/RecordingType.jsx:133–156` — only renders for `recordingType === "region"`, which is unreachable per the Region defer (entry above). The first `.popup-warning` block at lines 115–129 (gated by `contentState.updateChrome`) is live and uses the same .popup-warning shell minus the right-side learn-more link.
  - **`popup/layout/Settings.jsx` is now a 12-line wrapper** that only renders the Hide-toolbar Switch (F14 collapsed it). The `popup/styles/layout/_Settings.scss` file (34 lines) defines `.CollapsibleTrigger`/`.CollapsibleLabel`/`.CollapsibleRoot` classes with zero live JSX consumers — the Radix Collapsible primitive was removed when F14 flattened the row. Fix: delete `_Settings.scss` + its `@use` line in `popup/styles/_Popup.scss`.
- **Region recording mode — Region overlay + recordingType="region" branches across Recorder.jsx and friends** — discovered during F45 (Region overlay redesign) post-merge reachability audit. F6 (commit 29c9c9b, "drop Screen / Tab area popup tabs entirely") deleted the only UI path that ever flipped `chrome.storage.local.recordingType` to "region" — the two `Tabs.Trigger` elements in `Content/popup/layout/RecordingTab.jsx` whose `onValueChange={onValueChange}` wrote the new value to storage. F6's own DECISION comment explicitly flagged this: "With no UI to set recordingType to 'region', it stays at its 'screen' default, so the region branches in Recorder.jsx (tab capture path via chrome.tabCapture) are dead." Inert footprint: the whole `extension/src/pages/Content/region/` directory (`Region.jsx` rendering the resize-rect + 8 handles + dimmed mask, `RegionHandles.jsx` orphan, `components/`, `styles/_Region.scss`, the two orphan `styles/layout/_CameraToolbar.scss` + `_CameraWrap.scss` from a long-ago refactor); the `recordingType === "region"` branches in `Recorder.jsx` (tabCapture path), `Wrapper.jsx:318` (the `<Region />` gate), `popup/layout/RecordingType.jsx:131,187` (warning + custom-area switch), `Content/context/ContentState.jsx` (6+ branches in start/stop flows), `Content/warning/Warning.jsx:55`, `Content/cursor/trackClicks.js:86`; the `RegionDimensions` popup component; the `customAreaRecordingDisabled*` / `customAreaLabel` i18n keys. F45's commit (1c376a9) redesigned the overlay before this was verified and was reverted in the immediately-following commit. Fix: delete `Content/region/` entirely, the `<Region />` JSX in `Wrapper.jsx`, all `recordingType === "region"` branches across the codebase, the region-mode i18n keys, and the `regionX/regionY/regionWidth/regionHeight/customRegion/fromRegion/cropTarget` state plumbing in `ContentState.jsx` + `updateFromStorage.js`. Touches the recorder flow + content-script popup wiring which Phase 7 (backend deploy) needs stable, so deferred. Re-enable path: restore the two `Tabs.Trigger` elements in `RecordingTab.jsx` (see F6's diff for the exact JSX) if the team ever decides region cropping is worth the UX cost.
- **Screenity local-backup system — Backup page + initBackup pipeline branch + backup state plumbing** — discovered during F44 (Modal redesign) reachability audit. `chrome.storage.local.backup` is the gate (read in `Background/recording/desktopCapture.js:39`), initialized to `false` in `Content/context/utils/updateFromStorage.js:328` and `Background/listeners/onInstalledListener.js:58`, and **never set to `true` anywhere in the codebase** — the cog menu that used to toggle it was deleted in F31. Inert footprint: the entire `extension/src/pages/Backup/` directory (~744-line `Backup.jsx` + `index.html` + `index.jsx` + `messaging/`); `Background/backup/initBackup.js`; the `if (backup)` branch in `desktopCapture.js`; the `backup-error` message handler in `Content/context/messaging/handlers.js:757` (its modal is unreachable); the `backup`/`backupSetup` storage plumbing in `updateFromStorage.js`; the `backup.html` entry in manifest + webpack. Kept in E3 audit because reachability wasn't proven at that point. Fix: delete `extension/src/pages/Backup/`, `Background/backup/`, the `backup`/`backupSetup` state, the `desktopCapture.js` branch, the `backup-error` handler, and the `backup.html` manifest/webpack entries. Touches the Background recording-launch path which is exactly what Phase 7 (backend deploy) needs stable, so deferred.
- **Screenity onboarding system — driver.js popover + "Click here to draw" handwriting arrow** — discovered during F43/F44 planning, both are inert. Two distinct surfaces share the same root cause: Stage E cleanup removed their triggers.
  - **Driver.js popover** is gated by `chrome.storage.local.onboarding`, written on install in `onInstalledListener.js:28` as `onboarding: cloudFeaturesEnabled`. `cloudFeaturesEnabled` resolves to `process.env.SCREENITY_ENABLE_CLOUD_FEATURES === "true"` at build time via webpack DefinePlugin; we never set that env var, so the flag is always `false` and the tour never starts. Inert footprint: ~140 lines of unreachable popover styling at the bottom of `_Toolbar.scss` (block starting at `.driver-popover.ScreenityOnboardingPopover`) + mirrored in `app.css`; the `driver.js` and `driver.js/dist/driver.css` imports at the top of `proOnboarding.js`; the whole `Content/popup/onboarding/` directory; `onboarding` state plumbing in `PopupContainer.jsx`; the `desktopCapture.js:22` onboarding gate.
  - **"Click here to draw" handwriting arrow** is gated by `contentState.showOnboardingArrow`, which only flips to `true` in response to the `"setup-complete"` message. The receiver lives in `Content/context/messaging/handlers.js:702`, but nobody fires that message anymore — Stage E3 deleted the orphan `Setup/` directory that used to send it. The arrow JSX in `ToolbarWrap.jsx:526` and its styles (`.OnboardingArrow` / `.OnboardingText` / `.ArrowShape` in `_Toolbar.scss`, plus the Gloria Hallelujah `@font-face` block in `_variables.scss`) are reachable in code but never render at runtime.
  - F43 and F44 were originally scoped to *redesign* these surfaces with the Editorial Manual aesthetic; skipped after discovering they're never reachable in our build. Fix: delete the popover SCSS block + the `proOnboarding.js` file + the `driver.js` npm dependency + the `onboarding` state in `PopupContainer.jsx`; delete the arrow JSX block + its three CSS classes + the Gloria Hallelujah font asset + the orphan `setup-complete` handler. Touches the popup/messaging surface which is exactly what Phase 7 (backend deploy) needs stable, so deferred.

**Why deferred:** cleanup touches the Background messaging surface, which is exactly the layer Phase 7 (backend deploy) needs to validate as stable. Tackle these after the deploy is verified working end-to-end. Per-item decisions and rationale are in the git commit messages for D1–E5.

## Tech stack (see ARCHITECTURE.md for full list)

- **Backend:** Python 3.11+, FastAPI, Uvicorn
- **Media processing:** FFmpeg (invoked via `subprocess`, not a wrapper library)
- **AI services:** OpenAI Whisper API, OpenAI GPT-4o mini API
- **Document generation:** python-docx-template, Pillow
- **Frontend:** Chrome extension forked from Screenity (GPLv3, internal-use only)
- **Deployment:** Single Hetzner CPX22 (Ubuntu 24.04) via systemd + nginx + certbot

## Behavior notes for Claude

- When a decision is not covered by `ARCHITECTURE.md`, make the simplest choice consistent with the critical principles above, and leave a `# DECISION: <explanation>` comment at the point of decision in the code.
- At the end of each phase, summarize any such decisions made during that phase.
- Shared password auth only — never add OAuth, user accounts, or permission systems without explicit approval.
- The user is a newcomer to programming (see `~/.claude/CLAUDE.md` for global preferences). Use simple language, provide step-by-step commands, and ask for approval before refactoring.
