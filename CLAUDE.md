# InstructionsCrafter — Project Instructions

## What this project is

A tool that turns a screen recording with voice narration into a polished Microsoft Word step-by-step instruction document. A Chrome extension (forked from Screenity) records screen + audio and uploads to a FastAPI backend, which transcribes the audio with Groq Whisper Large V3 Turbo, segments it into logical steps, extracts one screenshot per step, polishes the narration with OpenAI GPT-5.4, and returns a `.docx` file. No long-term storage.

## Source of truth

**Full specification: `ARCHITECTURE.md`** in the project root.
Read it before making design decisions. Do not rewrite or duplicate the plan it contains.

## Critical principles (from ARCHITECTURE.md — must follow)

1. **Reuse over rewrite.** Assemble existing libraries with glue code. Do not reimplement what reputable libraries already do.
2. **Prefer boring technology.** FastAPI, FFmpeg, python-docx-template, OpenAI APIs. No exotic frameworks.
3. **Target 1,000–1,500 lines of custom code total.** Code growing far beyond this signals over-engineering.
4. **Internal tool for ≤5 users.** No multi-tenancy, RBAC, microservices, or enterprise observability.
5. **No long-term storage.** Videos and generated documents are deleted after each request. No database for user content.
6. **Ask before inventing.** If a requirement is ambiguous, state the interpretation explicitly before implementing.

## Development workflow

Work through the 9 phases in `ARCHITECTURE.md` **sequentially**. Do not start phase N+1 until phase N is verified working end-to-end per its listed verification step.

**Current phase: Phase 7 complete — backend live in production at `https://instructionscrafter.com` on Azure `Standard_D2s_v3` (East US, 24/7 always-on); end-to-end document generation verified 2026-05-25. Phase 8 prior: Stages A–E (functional) verified end-to-end against local backend; F-track (F1–F58, "Editorial Manual" visual redesign + branding) layered on top of that verification. A: Screenity fork imported into `extension/`. B: options page, post-recording Generate page, recording-blob loading from OPFS (materialized into memory at load time) / IndexedDB, multipart upload to `/api/generate`, .docx download. C: welcome page replacing Screenity's `setup.html`, permanent toolbar tooltip stating the narration requirement, audio-silence detection both client-side (hard-blocks the Generate button) and backend-side (ffmpeg `volumedetect` gate at -50 dB rejects silent recordings before Whisper hallucinates filler from them), Download recording button, backend CORS middleware. D (6 sub-commits, ARCHITECTURE.md lines 86–98): D1 deleted `Editor/`, `EditorWebCodecs/`, `EditorViewer/`, `Sandbox/` source dirs + GIF export + crash-recovery (~13k lines); D2 deleted `Background/drive/` + `Background/modules/signIn.js` + `oauth2`/`identity` from manifest; D3 removed the countdown on/off toggle (3-second duration was already hardcoded); D4 removed push-to-talk Switch + Alt+Shift+U listeners + state plumbing; D5 deleted `Camera/`, `Content/camera/`, `Content/camera-only/`, MediaPipe `vision_wasm*` assets (~22 MB), camera tab in popup, camera toggle button in toolbar; D6 moved shared OPFS chunk-store from `CloudRecorder/recorderStorage/` to `Background/chunkStorage/` then deleted `CloudRecorder/` in full (~9k lines) + `OffscreenRecorder/resumeJournal.js` + OffscreenRecorder cloud-branch. E (5 sub-commits, "surface cleanup"): E1 dropped `externally_connectable` from manifest; E2 flattened dead `recordingType === "camera"` branches in `Recorder.jsx`, dead `editorUrl === "editorviewer.html"` branch + duplicate-string ternary in `stopRecording.js`, and three `disabled={...camera}` props on toolbar `ToolTrigger`s; E3 deleted orphan `Setup/` (Stage C replaced its trigger with `Welcome/`) and the now-dead setup.html filter checks across `openRecorderTab.js`, `onActionButtonClickedListener.js`, `onCommandListener.js`, `Wrapper.jsx`, manifest, webpack — kept `Playground/` and `Backup/` after audit (both still actively opened); E4 stripped 173 unused i18n keys × 18 locales (~12k lines deleted; detected via `getMessage("KEY")` cross-reference); E5 npm-uninstalled 8 unused deps (`@mediapipe/tasks-vision`, `gif.js`, `plyr`, `plyr-react`, `react-advanced-cropper`, `fix-webm-duration`, `webm-duration-fix`, `@sentry/browser`) — kept `fabric` (still used by `Content/canvas/` for in-recording annotation overlay). End-to-end verified against local backend at the end of Stage E. F (58 commits, F1–F58, "Editorial Manual" visual redesign + branding): F1–F25 incremental popup/toolbar cleanups (trim redundant popup elements; drop legacy outer tabs, push-to-talk, mic-icon toggle, "no microphone" mic-dropdown option, external Screenity URLs / report-error senders / JSZip helper / NoInternet path; swap Screenity brand mark + favicon for InstructionsCrafter across all surfaces). F26–F42 introduced the shared Editorial Manual design system (Instrument Serif display + Geist body + Geist Mono numerals/caps + paper-tone palette `#F6F7FB` / `#FBFBFE` / ink `#15171C` / mid `#6E7684` / hairline `#E8E9EE` + accent `#3080F8` + danger `#C53030`) and applied it to all page-level surfaces (Welcome F26, Playground F27, post-recording Generate F37) and the in-recording surfaces (warning toast F29, popup shell + Start button F30, Countdown overlay F38, in-recording toolbar shell + recording controls + mode toggles F39, in-recording Toast + Tooltip F40, sub-toolbar popups Drawing/Blur/Cursor F41, Drawing tool internals + RadialMenu + ColorWheel + StrokeWeight F42). F43–F58 completed the sweep across remaining reachable surfaces: in-page Modal + EN "contact me" copy fix (F44), Region overlay (F45 — reverted after post-merge audit showed Region mode unreachable per F6's UI removal), Options page (F46), canvas selection-handle palette (F47), popup-internals via `_variables.scss` color/radius remap cascading to every live popup `.scss` file (F48), PopupContainer typography + accent rgba alignment (F49), both Radix dropdown primitives `.SelectContent` + `.ScreenityDropdownMenuContent` (F50), in-recording Tooltip cascade-override fix unblocking F40's redesign that had been silently shadowed by `Content.jsx`'s higher-specificity `.screenity-shadow-dom .TooltipContent !important` selector since F40 was committed (F51), Recorder iframe + audio warning toast (F53–F55: F53 introduced eyebrow + Geist + ink, F54 repositioned header above native Chrome picker, F55 removed the header entirely after browser testing confirmed it was effectively unseen), `Wrapper.jsx` shadow-DOM container baseline + LoadingOverlay font swap closing the F48 Satoshi-Medium gap (F57), and post-F-track font-loading audit fix adding regular `Geist:wght@300;400;500;600` to the `app.css` Google Fonts `@import` URL + stripping `Satoshi-Medium`/`Satoshi-Bold` from all 10 live fallback chains across 7 files (`app.css`, `app.scss`, `Wrapper.jsx`, `Content.jsx`, `_PopupContainer.scss`, `_Dropdown.scss`, `Recorder/warning/Warning.jsx`) — surfaces F30/F39/F44/F48–F50/F53 thought were Editorial-Manual-Geist had been silently rendering bundled Satoshi-Medium via fallback because regular Geist was never loaded in the shadow DOM, only Geist Mono + Instrument Serif (F58). F43 / F44 / F52 / F56 are documentation-only commits cataloguing dead/unreachable Screenity surfaces in the "Deferred cleanup" section below — together they cover the Backup page, Region overlay + state plumbing, driver.js onboarding popover, "Click here to draw" handwriting arrow, popup-internal Tooltip + cloud-gated layouts (LoggedOut.jsx / InactiveSubscription.jsx / WelcomeAlternate.jsx), Playground/index.html orphan `@font-face` declarations, dead popup class blocks (`.ExperimentalLabel` / `.alarm-time-button` / `.dashboard-button` / `.HelpSection` / `.popup-warning-right` / `.CollapsibleTrigger`/`.CollapsibleLabel`), 3 dead recorder i18n keys post-F55, and the broader ~600–800 lines of inert cloud-account code. Browser-verified during the F-track by the user: popup shell + mic dropdown + Hide-toolbar switch + Start button + in-recording floating toolbar (F30 + F39 + F48–F50), recorder iframe paper-tone surface + animated diagonal pattern + audio warning toast (F53), Modal in "Recording couldn't start" danger register (F44), Options page (F46), in-recording Discard-confirmation Modal (F44 covering the openModal shared API), the popup "RECORDER" mono-caps version label + Editorial Manual chrome (F30 + F49), and popup/in-recording toolbar/modal/warning surfaces now rendering in actual Geist after F58 (browser-confirmed the typographic difference vs the pre-F58 bundled-Satoshi silent fallback). Build-artifact-only verification (no live browser test by the user yet): F38 countdown overlay; F41 sub-toolbar popups (Drawing/Blur/Cursor); F42 drawing tool internals (RadialMenu/ColorWheel/StrokeWeight); F47 canvas selection handles (only visible during drawing mode + Select subtool); F51 in-recording Tooltip (now actually rendering after the cascade fix — needs a hover-on-toolbar-icon test to confirm visually); F57 Wrapper.jsx baselines. Every reachable visible surface is now on Editorial Manual; every remaining Screenity-styled byte is documented as inert in the deferred-cleanup section below. Phase 7 (deploy backend, 2026-05-25): provisioned Azure `Standard_D2s_v3` in East US (2 vCPU / 8 GB RAM / 32 GB Standard SSD LRS) with Ubuntu 24.04 LTS Gen2 + Trusted Launch in Availability Zone 1, Standard ARM Static Public IP `4.227.180.120`, resource group `SSI_Virtual_Machines`, tags `Project=InstructionsCrafter` / `Environment=Prod` / `Owner=Andrew Parshykov`; ssh-key-only auth (key at `~/.ssh/InstructionsCrafter_key.pem` on user's Mac). Installed `python3` + `python3-venv` + `python3-pip` + `ffmpeg` + `nginx` + `certbot` + `python3-certbot-nginx` + `fail2ban` via apt. Backend deployed to `/opt/instruction-generator/` (rsynced from local Mac, excluding `.env` / `venv` / `__pycache__` / `*.pyc` / `.DS_Store`); fresh Python venv with all 8 packages from `requirements.txt`. New `.env` created on the server with production `OPENAI_API_KEY` / `GROQ_API_KEY` / `SHARED_PASSWORD` / `MAX_VIDEO_SIZE_MB=500` / `TEMP_DIR=/tmp/instruction-generator` / `ALLOWED_ORIGINS=chrome-extension://iikpfdkhhgockbnndahjkmhocfgpolaa` (file mode 600). systemd service `instruction-generator.service` running uvicorn at `127.0.0.1:8000` under `User=azureuser`, `Restart=on-failure`, `Environment="PATH=/opt/instruction-generator/venv/bin:/usr/local/bin:/usr/bin:/bin"` — the trailing system paths were a Phase 7 gotcha: the initial unit file used only the venv bin and the extension's pre-flight health check reported "FFmpeg is not installed on the server" until `/usr/bin` was appended so `subprocess` calls to `ffmpeg` would resolve. nginx as reverse proxy with `client_max_body_size 500M`, `proxy_read_timeout 300s`, `server_name instructionscrafter.com www.instructionscrafter.com`, default site removed. UFW firewall enabled: default deny incoming / allow outgoing, explicit allow rules for 22 / 80 / 443 tcp (IPv4 + IPv6). fail2ban configured with `[sshd]` jail (maxretry 5, bantime 1h, `backend = systemd`) — note: writing config files via heredoc on Mac Terminal repeatedly hit a paste-indentation bug where the `EOF` terminator got leading whitespace and didn't close the heredoc; resolved by post-paste `sed 's/^[[:space:]]*//; /^EOF$/d'` cleanup. Domain `instructionscrafter.com` purchased via Namecheap with NEWCOM679 promo ($6.79 first year, $14.98/year renewal, auto-renew ON; Domain Privacy enabled free forever); two A records (`@` and `www`) → `4.227.180.120`. Let's Encrypt TLS cert covering both `instructionscrafter.com` and `www.instructionscrafter.com` via `certbot --nginx`; initial cert install failed because nginx config had `server_name _;` (wildcard) — fixed by updating to explicit domain names then `certbot install --cert-name instructionscrafter.com`; HTTP → HTTPS 301 redirect enabled; cert auto-renewal scheduled via systemd timer (90-day Let's Encrypt cycle). Chrome extension's stored `BACKEND_URL` updated from `http://127.0.0.1:8000` → `https://instructionscrafter.com` via the extension's options page. End-to-end verification 2026-05-25: recorded a short narrated screen capture via the extension, Generate Document succeeded over HTTPS, the `.docx` returned and downloaded — confirming the full pipeline (Whisper transcription via Groq, segmentation + polishing via OpenAI GPT-5.4, FFmpeg frame extraction, docxtpl render) runs in production. The VPN/Groq concern noted earlier is resolved: outbound API calls now originate from Azure East US, not the user's local network. Estimated all-in cost: ~$76/month (VM always-on $70.08 + disk $2.40 + IP $3.65 + bandwidth ~$0 under 100 GB/month free tier) + ~$14.98/year domain renewal (year 2 onward) + ~$10–30/year API usage = ~$940–960/year total budget. **Deferred to Phase 9 polish — see the "Deferred cleanup" section below.** **Next: Phase 9 (polish) — work through the deferred items in the "Deferred cleanup" section below (~600–800 lines of inert cloud code, dead i18n keys, popup-internal Screenity surfaces, Region/Backup/onboarding system removals, etc.), bump `extension/src/manifest.json:6` from `4.4.5` to a production version (likely `1.0.0`), and decide on Chrome Web Store distribution method (unpacked dev / unlisted / public).**
Update this line whenever the current phase changes.

## Deferred cleanup (Phase 9 polish backlog)

After Stage E, ~600–800 lines of inert Screenity cloud-account code remain in the tree. All gated at runtime by `CLOUD_FEATURES_ENABLED=false` (webpack DefinePlugin) or by no-data conditions, so none of it ships reachable behavior. Listed so a future maintainer doesn't re-discover them from scratch — each item names a non-obvious reason it wasn't deleted in Stages D/E.

- **`Background/auth/loginWithWebsite.js`** + 11 callers (CLAUDE.md originally listed 3; Phase 9 Tier B Batch E audit found 8 more in `Background/messaging/handlers.js` at lines 458 / 1380 / 1545 / 1555 / 1812 / 1843 / 2294 / 2308 / 2363 plus the self-recursive call at `loginWithWebsite.js:173`) — Screenity web-app login. Returns `{ authenticated: false, instantMode: false }` immediately when flag is off. Each caller destructures various combinations of `{ authenticated, subscribed, cached, transient, error, user, proSubscription, hasSubscribedBefore }` and branches on `if (authenticated)` / `if (refresh.authenticated)`. Flattening the function to a one-liner that returns the dead value is mechanical; flattening every caller's downstream `if (authenticated)` block is much larger work (~9 files in `messaging/handlers.js` alone, each with multiple lines of dead `if (auth?.authenticated) { ... }` blocks to remove). Deferred as a separate batch when ready. Quick-win fix when ready: replace the body of `loginWithWebsite` with `return { authenticated: false, instantMode: false };`, delete the rest of the file (the fetch helpers / refresh logic / cache checks), then flatten callers in a second pass.
- **~~`Background/listeners/onMessageExternalListener.js`~~ (Phase 9 Tier B Batch E — done)** — deleted. The whole listener early-returned on `!CLOUD_FEATURES_ENABLED` so was a 186-line no-op; doubly dead since E1 already dropped `externally_connectable` from the manifest, meaning external messages couldn't reach it anyway. File deleted + import/call removed from `Background/listeners/index.js`.
- **~~`Background/recording/restoreCloudRecording.js`~~ (Phase 9 Tier B Batch F — done)** — deleted (102-line file). Both `check-cloud-restore` and `restore-cloud-recording` registerMessage blocks in `Background/messaging/handlers.js` had zero senders anywhere in the tree (CLAUDE.md's `SettingsMenu.jsx:270` reference was stale — that sender no longer exists). Both functions imported (`checkCloudRestore`, `restoreCloudRecording`) and their 2 handler registrations removed.
- **~~`Background/recording/resumePendingUploads.js`~~ (Phase 9 Tier B Batch F — done)** — deleted (113-line file). Imports + calls removed from `onInstalledListener.js` (the wrapping 5-second setTimeout was the only thing the listener scheduled after the executeScripts block), `onActionButtonClickedListener.js` (single line at the top of the click handler), and `onStartupListener.js` (the 5-second setTimeout was the only body of the onStartup handler — kept the listener registration as a service-worker keep-alive hack per the existing comment).
- **~~`Background/index.js` `cleanupOrphanOpfsSessions`~~ (Phase 9 Tier B Batch F — done)** — function (~49 lines) + call site removed. The `listSessionDirs` / `destroySessionDir` imports from `./chunkStorage/opfsKvStore` (which were used ONLY by this function) also dropped. The stale comment reference in `Generate/loadRecording.js:128` rewrites to drop the function name.
- **~~Local-playback-offer machinery~~ (Phase 9 Tier B Batch G — done)** — ~831 lines removed across 4 files. Real scope was larger than the original "~200 lines" estimate: Background side (`Background/recording/cloudLocalPlaybackConstants.js` deleted; `Background/alarms/handleAlarm.js` `CLOUD_LOCAL_PLAYBACK_ALARM` branch + 2 import blocks + `chunksStore` import removed; `Background/messaging/handlers.js` had ~70 lines of constants/imports, 7 helper functions (`normalizeLocalPlaybackOffer`/`offerScreenStore`/`isLocalPlaybackOfferExpired`/`scheduleLocalPlaybackAlarm`/`getStoredLocalPlaybackOffer`/`clearStoredLocalPlaybackOffer`/`getValidLocalPlaybackOffer`) totaling ~170 lines, 6 `cloud-local-playback-*` registerMessage blocks totaling ~175 lines, plus a surgical edit to the `editor-ready` handler removing the `getValidLocalPlaybackOffer` fetch + the `localPlayback:` field in its forwarded `update-project-ready` message + the related logging fields); Content side (`Content/context/messaging/handlers.js` had 6 state vars, the `LOCAL_PLAYBACK_MAX_BYTES` constant, 5 helper functions (`revokeActiveLocalPlaybackSource`/`markLocalPlaybackFallback`/`fetchLocalPlaybackSourceFromExtension`/`ensureLocalPlaybackReady`/`postLocalPlaybackHandoff`) totaling ~150 lines, the `instructionscrafter-local-playback-request` branch of `onWindowProjectMessage` totaling ~95 lines, the `beforeunload` listener that called `revokeActiveLocalPlaybackSource`, the `if (activeLocalPlaybackSource?.url ...)` block in the `get-project-info` handler, and the local-playback portion of the `update-project-ready` handler (~75 lines). All unreachable in our build since CLOUD_FEATURES_ENABLED is always false and no Screenity web-app editor exists to send the triggering postMessages.
- **`isLoggedIn` / `isSubscribed` / `screenityToken` / `wasLoggedIn` / `proSubscription` / `hasSubscribedBefore` state plumbing in `Content/*`** — gates ~50+ UI conditional renders in popup, toolbar, settings. Flag-derived state is always `false` in our build. Deferred because flattening every `if (isSubscribed)` cleanly without breaking the popup layout is many-file work for zero behavioral gain.
- **`cameraActive` / `cameraPermission` / `defaultVideoInput` / `backgroundEffects*` state plumbing** — written to storage, read in many places, but no live consumer after D5 deleted the camera UI.
- **Broken `npm run build:content-css` script** — Stage D5 deleted `Content/camera/` and `Content/camera-only/`, but `src/pages/Content/styles/app.scss` still has `@use "../camera/styles/_Camera.scss";` and `@use "../camera-only/styles/CameraOnly.scss";`. **Scope is larger than originally believed.** Phase 9 audit (2026-05-25): running `sass src/pages/Content/styles/app.scss` after deleting those two `@use` lines compiles cleanly, but the regenerated `app.css` diverges from the live hand-edited `app.css` by ~388 lines: 323 lines exist only in hand-edited `app.css` (F58 Google Fonts `@import`, F30/F32 redesign comments, `border-radius: 30px` overrides in ~8 places where `_variables.scss` now says `8px`, ~118 lines of Screenity dashboard CSS — `.video-ui` / `.blurred` / `.videos-list` / `.ModalSoon` etc. — that was never migrated to `.scss`), and 65 lines exist only in `.scss`-regenerated output (newer Editorial Manual `border-radius: 8px` rules, F48 palette comments). So `app.css` has been the de facto source of truth since at least F30, with `.scss` sources stale. "Delete the two `@use` lines and the script becomes canonical again" is NOT true — running the script as-is would lose the ~118 dashboard lines + Google Fonts `@import` + F30/F32 comments and regress `border-radius: 30px` → `8px` in 8 places. Three real options: (a) **Full reconciliation** — port the 323 hand-edited additions back to the appropriate `.scss` source files (mostly `_variables.scss` + popup component `.scss` files + a new file for the dashboard CSS), decide whether the 65 `.scss`-only lines should be reapplied (they're newer Editorial Manual styling), then make the script canonical. Several hours of work; right architectural fix. (b) **Delete the script entirely** — remove `build:content-css` from `package.json` and declare `app.css` the explicit source of truth. Removes the footgun in one move; loses the ability to regenerate from `.scss` going forward. (c) **Status quo** — keep editing `app.css` by hand; the broken script self-protects by failing loudly. Decision deferred. Re: dashboard CSS specifically: the `.video-ui`/`.blurred`/`.ModalSoon` styles are likely also cloud-build-gated dead code (similar to the `_Welcome.jsx`/`_VideoItem.jsx` surfaces called out elsewhere in this list); cleaning them up belongs with whichever reconciliation path is chosen.
- **Editorial Manual fonts loaded via Google Fonts CDN inside the content-script shadow DOM** (introduced in F38 for the countdown redesign; expanded in F58 to also load regular Geist for the popup/toolbar/modal/warning surfaces that the F-track redesigned but had been silently rendering bundled Satoshi via fallback) — `src/pages/Content/styles/app.css` starts with `@import url("https://fonts.googleapis.com/css2?family=Geist+Mono:wght@400;500&family=Geist:wght@300;400;500;600&family=Instrument+Serif:ital@0;1&display=swap")`. On host pages with strict CSP that block external font fetches (some banking sites, locked-down Salesforce orgs, etc.), the fetch is blocked and all in-page UI text (popup, in-recording toolbar, modals, audio-warning toast, Radix dropdowns) falls back to `-apple-system` / system fonts; the countdown specifically falls back to `Times New Roman` italic for the numeral and system monospace for the cancel hint. All fallbacks render acceptably at the sizes we're using, but the typography isn't visually pixel-identical to the Welcome/Generate pages. F58 deliberately removed `Satoshi-Medium`/`Satoshi-Bold` fallback-chain entries across every live shadow-DOM surface (the bundled Satoshi safety net was masking the Geist-not-loaded bug during F-track verification), so strict-CSP now degrades straight to system fonts rather than to Satoshi. Fix: bundle the `.woff2` files locally under `extension/src/assets/fonts/` (mirror the Satoshi pattern already there for Screenity's bundled fonts), declare `@font-face` blocks pointing to `chrome-extension://__MSG_@@extension_id__/assets/fonts/...` URLs at the top of `app.css`, and drop the Google Fonts `@import`. Same fix would benefit any future in-recording UI redesign (toolbar, modal, etc.) that wants the Editorial Manual fonts.
- **`Playground/index.html` orphan Satoshi + Gloria-Hallelujah `@font-face` declarations** — discovered during the post-F55 design audit. The Playground page (F27 redesign) loads Geist + Geist Mono + Instrument Serif via a Google Fonts `<link>` and uses only those fonts in `Playground.jsx`'s inline styles, but the HTML still has four `@font-face` blocks loading `Satoshi-Light.ttf` / `Satoshi-Medium.ttf` / `Satoshi-Bold.ttf` / `GloriaHallelujah-Regular.ttf` from `assets/fonts/`. Dead bytes — these family names are never referenced in any `font-family:` declaration on the page. The same .ttf files remain bundled because other pages still reference them as fallbacks (or are themselves dead — Backup page, popup-internal `_variables.scss` etc.). Fix: delete the entire `<style>` block at `Playground/index.html:14-44`.
- **7 dead recorder i18n strings** — `recorderSelectTitle`, `recorderSelectProgressTitle`, `recorderSelectDescription` lost their only consumer when F55 removed the recorder iframe's `.middle-area` block. `recordAudioWarningMacTitle`, `recordAudioWarningMacDescription`, `recordAudioWarningOtherTitle`, `recordAudioWarningOtherDescription` lost their only consumer when `Recorder/warning/Warning.jsx` was deleted alongside the picker's `"audio"` source request (the "Record computer audio" toast became misinformation once the toggle was hidden, since F31's `systemAudio: false` already meant the captured tab/system audio was being stopped immediately at `Recorder.jsx:3234–3239` and never reached the recording — the toggle was a no-op). The 8th candidate (`preparingLabel`) is still live at `Wrapper.jsx:27` (the LoadingOverlay spinner's "Preparing..." label during shadow-DOM mount), so it stays. The seven dead keys exist in all 18 locale files (en, es, fr, de, ja, etc.) — same shape as the 173 keys swept in Stage E4. Fix: include in the next batch i18n sweep alongside any other dead keys discovered. Single piecemeal edit would be 126 file touches; better batched.
- **Popup-internal Screenity surfaces left after F48–F51** — discovered during the popup-internals audit. All inert in our build because their JSX consumers are cloud-gated, alarm-feature-removed (F31), region-mode-removed (F45 revert), or were orphaned by earlier F-cleanups (F4 tabs, F6 Screen/Tab tabs, F14 Settings collapsible, F31 cog menu). Listed grouped by file:
  - **`extension/src/pages/Content/popup/styles/components/_Tooltip.scss`** (99 lines) + its compiled block in `app.css:3279` — the popup-internal Radix Tooltip. Its only JSX consumer (`popup/components/TooltipWrap.jsx`, used at `popup/layout/RecordingType.jsx:203`) is gated by `CLOUD_FEATURES_ENABLED && contentState.isLoggedIn`. The class name `.TooltipContent` clashes with the F40-redesigned in-recording toolbar Tooltip; F51 harmonized the runtime override in `Content.jsx` so the toolbar tooltip finally renders correctly, but this dead popup .scss block remains in the cascade and would re-shadow F40 if `Content.jsx`'s override is ever removed. Fix: delete the file + the `@use` line in `popup/styles/_Popup.scss` + the `popup/components/TooltipWrap.jsx` file + the `TooltipWrap` import line in `popup/layout/RecordingType.jsx`.
  - **`popup/styles/components/_Switch.scss .ExperimentalLabel`** (~10 lines, lines 78–86) — Beta-feature pill. The `experimental` prop on `<Switch>` is referenced in `popup/components/Switch.jsx:32` but no live caller ever passes `experimental={true}`. Used to flag experimental popup toggles in Screenity (e.g., fast-recorder beta); we removed all such toggles. Fix: delete the `<span className="ExperimentalLabel">` block in `Switch.jsx` and the corresponding .scss block.
  - **Background-side alarm machinery (popup-side removed in Phase 9 Tier B Batch C)** — Phase 9 audit revealed the alarm system extended into 6+ Background files beyond what CLAUDE.md originally described. Popup-side cleanup completed: `.alarm-time-button` JSX + alarmTime mm:ss math removed from `popup/layout/RecordingType.jsx`; `.alarm-time-button` .scss block removed from `_MainButton.scss` + compiled `app.css`; `alarm`/`alarmTime`/`fromAlarm` state defaults + the 4 `if (contentStateRef.current.alarm)` branches removed from `ContentState.jsx`; `alarm`/`alarmTime` storage-key entries + the dead `if (result.alarm)` initial-timer branch removed from `updateFromStorage.js`. Background-side **still inert but present** (deferred to a future polish round because it touches the recording-start critical path that Phase 7 verified working in production): `Background/recording/startRecording.js:215-309` destructures and persists `alarm`/`alarmTime` + creates a `chrome.alarms.create("recording-alarm", ...)` if alarm is truthy; `Background/recording/stopRecording.js:200,412` calls `chrome.alarms.clear("recording-alarm")`; `Background/alarms/handleAlarm.js:200-210` handles the `recording-alarm` name in the dispatcher (other alarm names like `RECORDER_KEEPALIVE_ALARM` are live and must stay); `Background/messaging/handlers.js:1535` registers `add-alarm-listener` (live — also installs the heartbeat/watchdog listener; do NOT remove); `Background/messaging/handlers.js:2332-2333` registers `clear-recording-alarm` (the popup-side senders for this were removed in Batch C, so this is now an orphan handler); `Background/listeners/onTabUpdatedListener.js:68-71` and `Background/listeners/onTabActivatedListener.js:76-79` both read `alarm` + `alarmTime` from `chrome.storage.local` to compute remaining time on tab focus. Since no code anywhere ever writes `alarm: true`, all these branches are dead at runtime — but removing them safely requires touching `startRecording.js`, which is the Phase 7-verified critical path. Fix path when ready: delete the `alarm`/`alarmTime` destructuring + persistence + `chrome.alarms.create` block in `startRecording.js`; delete the 2 `chrome.alarms.clear` calls in `stopRecording.js`; delete the `recording-alarm` branch in `handleAlarm.js`; delete the `clear-recording-alarm` registerMessage block + the alarm reads in both tab-listener files.
  - **`popup/styles/components/_MainButton.scss .dashboard-button`** (~10 lines, lines 47–54) — Screenity "Go to dashboard" button styling. Only consumed by `popup/layout/Welcome.jsx:117,149` and `popup/layout/WelcomeAlternate.jsx:80`, both of which are gated by `isCloudBuild`. Fix: delete with the rest of the dead popup Welcome surfaces.
  - **`popup/styles/layout/_PopupContainer.scss .HelpSection`** (~25 lines, lines 198–224, Screenity-era 30px pill in `#edeef2`) — no live JSX consumer at all. Pure orphan, probably from a long-deleted Screenity help-text bar.
  - **`popup/styles/layout/_PopupContainer.scss .popup-warning-right`** + the second `.popup-warning` JSX block in `popup/layout/RecordingType.jsx:133–156` — only renders for `recordingType === "region"`, which is unreachable per the Region defer (entry above). The first `.popup-warning` block at lines 115–129 (gated by `contentState.updateChrome`) is live and uses the same .popup-warning shell minus the right-side learn-more link.
  - **`popup/layout/Settings.jsx` is now a 12-line wrapper** that only renders the Hide-toolbar Switch (F14 collapsed it). The `popup/styles/layout/_Settings.scss` file (34 lines) defines `.CollapsibleTrigger`/`.CollapsibleLabel`/`.CollapsibleRoot` classes with zero live JSX consumers — the Radix Collapsible primitive was removed when F14 flattened the row. Fix: delete `_Settings.scss` + its `@use` line in `popup/styles/_Popup.scss`.
  - **Remaining cloud-gated popup surfaces (Phase 9 Tier B Batch D partial)** — Batch D deleted the 3 named layout files (`LoggedOut.jsx`, `InactiveSubscription.jsx`, `WelcomeAlternate.jsx`) + their imports in `PopupContainer.jsx` + the 2 `isCloudBuild &&` ternary branches that gated them (at PopupContainer.jsx:522 and 552 pre-edit). The ternary chain that used to span Welcome / InactiveSubscription / LoggedOut / RecordingTab is now just Welcome / RecordingTab. **Still cloud-gated and deferred** (kept in tight Batch D scope; pulling them into Batch D would have doubled the diff): (a) `popup/layout/Welcome.jsx` — transitively cloud-gated via `showWelcomeSplash` at PopupContainer.jsx:140-149 which requires `isCloudBuild && !isLoggedIn && !wasLoggedIn`. (b) `popup/styles/layout/_Welcome.scss` + its `@use "./layout/_Welcome";` in `_Popup.scss` — only consumed by Welcome.jsx. (c) `.dashboard-button` class in `_MainButton.scss:47-54` + compiled `app.css` — only consumers are `Welcome.jsx:117,149`; both unreachable since Welcome itself is unreachable. (d) The two `useEffect` blocks at PopupContainer.jsx:368-397 and 399-433 that call `runProPopupOnboardingIfNeeded` / `runProCameraOnboardingIfNeeded` — both early-return on `!isCloudBuild || !isPro`, so always no-ops. The `proOnboarding.js` module they import also drives the driver.js popover documented in the Screenity onboarding entry below. (e) The `isCloudBuild` constant declaration at PopupContainer.jsx:44 itself + its remaining 5 references (the `showWelcomeSplash` boolean + the 4 references inside the two cloud-gated useEffects + their deps lists). Fix when ready: delete Welcome.jsx + _Welcome.scss + its @use + the .dashboard-button class + the two pro-onboarding useEffects + the showWelcomeSplash boolean + the `<Welcome />` ternary branch (the chain collapses to a plain `<RecordingTab />`) + the `isCloudBuild` const + the related state plumbing (`onboarding`/`showProSplash`/`learntAboutPro`/`firstTimePro` storage keys, the proOnboarding module + its driver.js dep, the popup-side `isLoggedIn`/`isSubscribed`/`wasLoggedIn`/`proSubscription`/`hasSubscribedBefore`/`screenityUser`/`stayLoggedOut` chrome.storage entries and ContentState fields, the `runProPopupOnboardingIfNeeded`/`runProCameraOnboardingIfNeeded` exports). That follow-up touches the popup/messaging surface broadly so deserves its own audit pass.
- **~~Region recording mode~~ (Phase 9 Tier C Batch J — done)** — entire Region/tab-capture path removed. ~4,400 lines deleted across 58 files. Wide-scope cleanup: deleted both `extension/src/pages/Content/region/` (the in-page region-selection overlay UI — Region.jsx + RegionHandles.jsx + styles) AND `extension/src/pages/Region/` (the 2,547-line iframe-based recorder that handled tab/region capture — Region/Recorder.jsx + index.html + index.jsx). Also deleted: `Background/tabManagement/tabHelpers.js` (its sole export `setMicActiveTab` was part of the dead set-mic-active-tab message chain that only fired for region recordings), the popup-internal `RegionDimensions` component + its `_RegionDimensions.scss` + its `@use` in `_Popup.scss`. Webpack: dropped the `region` entry. Manifest: removed `region.html` from web_accessible_resources. Wrapper.jsx: dropped the `<Region />` JSX gate, the regionCaptureRef + its useEffect, the preloaded iframe block that loaded `region.html` (or `cloudrecorder.html` for the never-reachable subscribed path), the `import Region from "./region/Region"`, and the `contentState.customRegion` deps + the `!contentState.customRegion` outer-click gate. ContentState.jsx: dropped 7 state defaults (customRegion, regionWidth, regionHeight, regionX, regionY, fromRegion, cropTarget), the `recordingType === "region"` + `region: true/false` + `customRegion` branches in startRecording/restartRecording/stop/dismiss flows, the cropTarget postMessage to the now-gone iframe, the customRegion useEffect, and the `recordingType === "tab" || === "region"` isTabBound branch in the recording-state listener (collapsed to unconditional setContentState). updateFromStorage.js: dropped customRegion/regionWidth/regionHeight/regionX/regionY from storage keys + state mappings. popup/layout/RecordingType.jsx: dropped the cropActive state + the CropTarget useEffect, the two `recordingType === "region"` popup-warning + customRegion-switch JSX blocks, the RegionDimensions import. Content/warning/Warning.jsx: dropped the recordingType-region AudioIcon-suppression useEffect. Content/cursor/trackClicks.js: rewrote — dropped the isRegion/regionWidth/regionHeight/regionX/regionY signature params, the `region: relativeToRegion` field, the in-region click-coordinate translation, and the `cachedRecordingType === "region"` isTab payload field. Background recording layer: openRecorderTab.js heavily simplified — dropped `isRegion` param, the customRegion branch in startRecorderSession (collapsed `if (request.region)` to just the non-region path), the `?tab=true` URL suffix, the recordingTab/tabRecordedID region-specific writes; desktopCapture.js dropped region/customRegion perfMark fields; startRecording.js dropped the `recordingType === "region" || === "tab"` recordingMeta-tab block + region/customRegion storage reads + the customRegion ternary in start-recording-tab message; stopRecording.js + cancelRecording.js dropped the wasRegion → `{ wasRegion: false, region: true }` revert logic + region/customRegion clears; discardRecording.js + recordingHelpers.js dropped region/customRegion clears. Background/index.js: dropped the entire `region/customRegion` storage check + the "Stale region state on startup" warning + the `isRegion = Boolean(region) && !customRegion` branch in recoverInFlightRecording. lifecycleObserver.js: dropped region/customRegion observed keys. Background/listeners: onTabActivatedListener.js dropped the region/customRegion gate + recordingType-region check; onTabUpdatedListener.js dropped customRegion from the storage read; Background/messaging/handlers.js dropped the entire `region-iframe-destroyed` registerMessage (was the recorder-tab-pagehide handler for the deleted Region iframe) + the `set-mic-active-tab` registerMessage + the setMicActiveTab import. Recorder/Recorder.jsx: dropped the `set-mic-active-tab` message branch. Content/popup/components/Switch.jsx: dropped the `props.value === "customRegion"` post-change branch (was forcing `region: true` whenever the customRegion switch was flipped on). Content/images/popup/images.js: dropped the RegionTabOn/RegionTabOff SVG declarations + their exports. Asset/style cleanup: dropped `.region-dimensions` + `.region-input*` + `.region-recording` CSS blocks from app.css; dropped `@use "../region/styles/_Region.scss"` from app.scss; dropped `popup-warning-right` CSS rules (only consumer was the deleted region-mode popup warning); dropped `region.html` path-classifier branch from `pages/utils/perfMarks.js`; dropped region/customRegion from diagnosticLog observed keys. i18n: removed 6 region-related keys (customAreaRecordingDisabledTitle/Description/Action + customAreaLabel + regionWidthLabel + regionHeightLabel) × 18 locales = 108 blocks. **Deferred to a future polish round** (left in place since `recordingType === "region"` branches are now permanently unreachable but removing them touches deeper recording-flow logic in the recording-launch critical path): Recorder/Recorder.jsx still has `backupRef = useRef(false)` declarations + `if (backupRef.current)` dead branches (also flagged in the Backup deferred item), some `region: false` defensive storage writes remain in error-cleanup paths, and pages/utils/errorCodes.js + lifecycleLog.js still have region-mentioning constant strings that are harmless metadata. The `click-event` registerMessage in Background/messaging/handlers.js still destructures `region, isTab` from payload but the whole handler is gated by `CLOUD_FEATURES_ENABLED` (always false) so it's doubly-dead — cleanup belongs with the broader cloud-features sweep.
- **~~Screenity local-backup system~~ (Phase 9 Tier C Batch I — done)** — entire feature removed. Scope was significantly larger than the original audit predicted (~30 file touches). Deleted: full `extension/src/pages/Backup/` directory (Backup.jsx + index.html + index.jsx + messaging/), full `Background/backup/` directory (initBackup.js), `assets/backup-favicon.ico`, `assets/fonts/GloriaHallelujah-Regular.ttf` (now fully orphaned after the F-track `.OnboardingText` removal in Batch H and Backup/index.html deletion). Webpack: removed the `backup` entry + the favicon-only-for-backup conditional + the BS_DROPPED_ENTRIES entry. Background recording: dropped `backup`/`backupSetup` storage reads + writes from `desktopCapture.js` (flattened the `if (backup) initBackup() else startRecorderSession()` branch to just the recorder-session path; also dropped now-orphan `getCurrentTab` + `localDirectoryStore` imports); dropped backup-related stale-clear keys in `index.js`; dropped `backup`/`backupSetup`/`backupTab` from observed keys in `lifecycleObserver.js`; dropped backup storage keys + diag from `startRecording.js`; dropped `backup` parameter from `openRecorderTab` + the `startRecorderSession` storage read; dropped backup state clears + `discard-backup` message sends from `discardRecording.js` + `cancelRecording.js`; dropped the `backup-error` branch in `recordingHelpers.js`; dropped the entire `handleStopRecordingTabBackup` function in `stopRecording.js` + its import + `stop-recording-tab-backup` registerMessage; dropped the `backup-created` registerMessage; dropped the install-time `backup: false, backupSetup: false` write in `onInstalledListener.js`; dropped the `tab.url.includes("backup.html")` gate in `onTabActivatedListener.js`; dropped the `backup.html` path check in `perfMarks.js`. Content side: dropped `backup`/`backupSetup` defaults + the `discard-backup-restart` runtime message send from `ContentState.jsx`; dropped `backup`/`backupSetup` storage keys + state mappings + initial-set blocks from `updateFromStorage.js`; dropped the `backup-error` registerMessage handler from `Content/context/messaging/handlers.js`; dropped the `chrome.storage.local.get(["backup"])` reads + the `request.backup` assignments in `Recorder/Recorder.jsx` + `Region/Recorder.jsx`. i18n: removed all 14 backup-related keys (backupsTitle/Description1/2/SelectFolder/NotNow/OnTitle/OnDescription/Close/Stop/ConfirmTitle/ConfirmDescription/ConfirmAllow + backupPermissionFailTitle/Description) × 18 locales = 252 blocks. Fonts: removed Gloria-Hallelujah `@font-face` from `assets/fonts/fonts.css` (no remaining users). **Deferred to a future polish round** (out-of-scope for Batch I; the cleanup left these inert): `backupRef = useRef(false)` declarations remain at `Recorder/Recorder.jsx:334` and `Region/Recorder.jsx:146`, plus three `if (backupRef.current)` branches at `Recorder/Recorder.jsx:757` + `Recorder/Recorder.jsx:1468` + `Region/Recorder.jsx:421` — `backupRef.current` now never flips from its `false` default since the writers are gone, so the branches are dead code. Removing them requires deleting substantial backup-mode chunk-saving logic in the Recorder files which is in the recording-launch critical path; saved for a dedicated follow-up.
- **~~Screenity onboarding system~~ (Phase 9 Tier C Batch H — done)** — both onboarding surfaces removed. Deleted entire `Content/popup/onboarding/` directory (proOnboarding.js + storage.js); the driver.js + driver.js/dist/driver.css imports went with it. Removed the popover SCSS block from `_Toolbar.scss` and `app.scss` (two separate ~140-line blocks); removed the compiled equivalents from `app.css` (~380 lines across two locations — once compiled from `_Toolbar.scss` and once from `app.scss`'s own version). Removed the "Click here to draw" arrow: the JSX block + the `OnboardingArrow` SVG import in `ToolbarWrap.jsx`, the `OnboardingArrow` component definition + export in `SVG.jsx`, the `.OnboardingArrow`/`.OnboardingText`/`.ArrowShape` CSS classes + the `body.driver-active .OnboardingArrow` cross-feature hide rule, the Gloria-Hallelujah `@include font(...)` in `_variables.scss` + its compiled `@font-face` block in `app.css`. Also removed the orphan `setup-complete` registerMessage handler in `Content/context/messaging/handlers.js`, the `showOnboardingArrow` default + intermediate setter in `ContentState.jsx`, the `showOnboardingArrow: false` clearing in `ToolbarWrap.jsx`'s `handleChange`, the `clickHereDrawOnboarding` i18n key across 18 locales, the import + the two cloud-gated `useEffect`s in `PopupContainer.jsx` that called `runProPopupOnboardingIfNeeded` / `runProCameraOnboardingIfNeeded` (and the now-orphan `wasCameraActiveRef` they used), and the `onboarding` gate at `Background/recording/desktopCapture.js:22` (which also dropped `"onboarding"` from the chrome.storage.local.get keys). The Gloria-Hallelujah `.ttf` file remains bundled because `Backup/index.html:33-34` still references it; that reference dies with the Screenity local-backup system removal (next Tier C item). The `driver.js` npm dependency at `extension/package.json` is now an unused dependency; **user should run `cd extension && npm uninstall driver.js`** to clean it up. The `onboarding`/`showProSplash` storage keys + their reads in `PopupContainer.jsx` were KEPT (out-of-scope; tied to the deferred Welcome.jsx flow).
- **Extension version `4.4.5` in `extension/src/manifest.json:6` + Chrome Web Store distribution decision** — version string inherited from Screenity at fork time; the field is independent of the Chrome-assigned dev extension ID (`iikpfdkhhgockbnndahjkmhocfgpolaa` for the current local install) and can be edited at any time. Two decisions deferred to Phase 9 because they belong with the release, not with the in-progress F-track / Phase 7 work: (a) distribution method — keep distributing as unpacked (zero-cost, current behavior; matches ARCHITECTURE.md Phase 9 wording "Package the extension for distribution (.crx or unpacked for dev mode)"), Chrome Web Store unlisted (one-time $5 dev registration fee, private link only the team can install from), or Chrome Web Store public; (b) version number — `1.0.0` is the conventional first-release choice, or `0.1.0` if early production use should read as pre-release. Manifest version format: 1–4 dot-separated integers, each in `0–65535`. Fix once (a) is chosen: edit `extension/src/manifest.json:6` to the new version. Bumps thereafter on every release.

**Why deferred:** cleanup touches the Background messaging surface, which is exactly the layer Phase 7 (backend deploy) needs to validate as stable. Tackle these after the deploy is verified working end-to-end. Per-item decisions and rationale are in the git commit messages for D1–E5.

## Tech stack (see ARCHITECTURE.md for full list)

- **Backend:** Python 3.11+, FastAPI, Uvicorn
- **Media processing:** FFmpeg (invoked via `subprocess`, not a wrapper library)
- **AI services:** Groq Whisper Large V3 Turbo API (transcription), OpenAI GPT-5.4 API (segmentation + screenshot selection + polishing)
- **Document generation:** python-docx-template, Pillow
- **Frontend:** Chrome extension forked from Screenity (GPLv3, internal-use only)
- **Deployment:** Single Hetzner CPX22 (Ubuntu 24.04) via systemd + nginx + certbot

## Behavior notes for Claude

- When a decision is not covered by `ARCHITECTURE.md`, make the simplest choice consistent with the critical principles above, and leave a `# DECISION: <explanation>` comment at the point of decision in the code.
- At the end of each phase, summarize any such decisions made during that phase.
- Shared password auth only — never add OAuth, user accounts, or permission systems without explicit approval.
- The user is a newcomer to programming (see `~/.claude/CLAUDE.md` for global preferences). Use simple language, provide step-by-step commands, and ask for approval before refactoring.
